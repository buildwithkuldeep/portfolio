import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Next.js 16 has no stable config to disable Turbopack
  // So we keep the config empty and clean.
};

export default nextConfig;
